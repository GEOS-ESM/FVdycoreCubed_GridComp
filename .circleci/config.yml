version: 2.1

# Anchors in case we need to override the defaults from the orb
#baselibs_version: &baselibs_version v8.32.0
#bcs_version: &bcs_version v12.0.0

orbs:
  ci: geos-esm/circleci-tools@5

workflows:
  build-test:
    jobs:
      # We are turning off the GEOSgcm build and test for now, as the release/MAPL-v3
      # branch in GEOSgcm is still based on v11, but the FV3 code in this repo is now based
      # on v12. Once GEOSgcm MAPL3 is updated to v12, we can re-enable these tests.
      #########################################################################################
      # - ci/build:                                                                           #
      #     name: build-GEOSgcm-on-<< matrix.compiler >>                                      #
      #     context:                                                                          #
      #       - docker-hub-creds                                                              #
      #     matrix:                                                                           #
      #       parameters:                                                                     #
      #         compiler: [gfortran, ifort]                                                   #
      #     #baselibs_version: *baselibs_version                                              #
      #     repo: GEOSgcm                                                                     #
      #     checkout_fixture: true                                                            #
      #     mepodevelop: false                                                                #
      #     checkout_mapl3_release_branch: true                                               #
      #     persist_workspace: true # Needs to be true to run fv3/gcm experiment, costs extra #
      # - ci/run_fv3:                                                                         #
      #     name: run-FV3-on-<< matrix.compiler >>-with-GEOSgcm                               #
      #     context:                                                                          #
      #       - docker-hub-creds                                                              #
      #     matrix:                                                                           #
      #       parameters:                                                                     #
      #         compiler: [gfortran, ifort]                                                   #
      #     #baselibs_version: *baselibs_version                                              #
      #     requires:                                                                         #
      #       - build-GEOSgcm-on-<< matrix.compiler >>                                        #
      #     repo: GEOSgcm                                                                     #
      # # Run AMIP GCM (1 hour, no ExtData)                                                   #
      # - ci/run_gcm:                                                                         #
      #     name: run-GCM-on-<< matrix.compiler >>                                            #
      #     context:                                                                          #
      #       - docker-hub-creds                                                              #
      #     matrix:                                                                           #
      #       parameters:                                                                     #
      #         compiler: [gfortran, ifort]                                                   #
      #     requires:                                                                         #
      #       - build-GEOSgcm-on-<< matrix.compiler >>                                        #
      #     repo: GEOSgcm                                                                     #
      #     #baselibs_version: *baselibs_version                                              #
      #     #bcs_version: *bcs_version                                                        #
      #########################################################################################

      # Note: Because of CircleCI matrix limitations, we need to duplicate the build job for R4 and R8.
      # This is so we can only do the regression tests for the R4 builds, which are the only ones
      # it works for at the moment
      - ci/build:
          name: build-GEOSfvdycore-as-R4-on-<< matrix.compiler >>
          context:
            - docker-hub-creds
          matrix:
            parameters:
              compiler: [ifort, gfortran]
          fv_precision: "R4"
          #baselibs_version: *baselibs_version
          repo: GEOSfvdycore
          checkout_fixture: true
          # MAPL3 code uses a special branch for now.
          fixture_branch: release/MAPL-v3
          mepodevelop: false
          run_regression_tests: true
          ctest_options: "-L REGRESSION"
          persist_workspace: false # Needs to be true to run fv3/gcm experiment, costs extra
          # The regression tests need a larger machine to run in a reasonable time, so we set the resource class to xlarge for this job
          resource_class: xlarge
      - ci/build:
          name: build-GEOSfvdycore-as-R8-on-ifort
          context:
            - docker-hub-creds
          compiler: ifort
          fv_precision: "R8"
          #baselibs_version: *baselibs_version
          repo: GEOSfvdycore
          checkout_fixture: true
          # MAPL3 code uses a special branch for now.
          fixture_branch: release/MAPL-v3
          mepodevelop: false
          persist_workspace: false # Needs to be true to run fv3/gcm experiment, costs extra
      # We also turn off the FV3 dycore runs for now
      #############################################################################################
      # - ci/run_fv3:                                                                             #
      #     name: run-FV3-as-<< matrix.fv_precision >>-on-<< matrix.compiler >>-with-GEOSfvdycore #
      #     context:                                                                              #
      #       - docker-hub-creds                                                                  #
      #     matrix:                                                                               #
      #       parameters:                                                                         #
      #         compiler: [gfortran, ifort]                                                       #
      #         fv_precision: ["R4", "R8"]                                                        #
      #     #baselibs_version: *baselibs_version                                                  #
      #     requires:                                                                             #
      #       - build-GEOSfvdycore-as-<< matrix.fv_precision >>-on-<< matrix.compiler >>          #
      #     repo: GEOSfvdycore                                                                    #
      #############################################################################################
